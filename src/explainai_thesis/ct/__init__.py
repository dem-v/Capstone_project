"""Head-CT hemorrhage pilot (Phase 5.4).

The gate-first Branch-A path is now active for the PhysioNet `ct-ich` pilot:
locked HU I/O, the DifeiT binary hemorrhage wrapper (`1 - P(normal)`),
input-space attribution dispatch, and reusable per-case visualization helpers
live under this package.  CAM-family methods are still intentionally absent
for the ViT branch until a controlled token-to-grid design is introduced.
"""
